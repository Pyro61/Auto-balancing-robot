void NMI_Handler(void)
{
    while(1);   /* Currently only for debug purposes */
}



void HardFault_Handler(void)
{
    while(1);   /* Currently only for debug purposes */
}

/* SVC_Handler and PendSV_Handler defined in FreeRTOS kernel files */